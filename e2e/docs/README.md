# e2e/docs — topic index

Deep dives that don't belong in [CLAUDE.md](../CLAUDE.md) (the map) or the
package [README.md](../README.md) (the overview). Referenced from
`CLAUDE.md`'s "Further reading" section as a conditional link, so it loads
only when a task actually touches the topic — not on every session.

One file per topic (`hermetic-stack.md`, `agent-browser-cli.md`, …). Empty
for now — add a file here when a piece of e2e behavior needs more
explanation than a map entry, and link it from `CLAUDE.md`.
