/** Width of the fixed left rail (matches Skills/Agents list rails). */
export const RAIL_WIDTH = 300;

/** Skeleton rows shown while the discoverable set is loading. */
export const SKELETON_ROWS = 5;
