# client/ — engineering insights

Append-only. Newest entry on top within each section. Never edit or delete
existing entries. Promote anything that becomes a standing rule into
[CLAUDE.md](CLAUDE.md) instead of leaving it here.

Entry test: if it'd be obvious to anyone reading the code, don't write it.
Each entry must be specific enough that a cold agent knows exactly what to
do without re-investigating.

## What Works

- _2026-08-22_ — **Fixed the SPEC-04 fix-mode Gap 2 ("the collapsed case row does not state the expectation type") by adding the `caseModal.kind.*` statement to `CaseRow`'s ALWAYS-VISIBLE `rowInfo` block in `EvalsTab.tsx`, and deliberately left the expanded panel's own "Expected outcome" heading+statement untouched** — removing it would violate AC-55 ("presents, alongside that case's expectation, the findings the agent returned"), a separate requirement for the OPEN panel. Net effect accepted on purpose: once a row is opened, the exact sentence (e.g. "Must find — the agent should flag this") now renders TWICE on screen (row header + panel), which breaks two pre-existing tests via `getByText`'s "multiple elements" error — `evals_tab_case_row_states_pass_kind_severity_and_category` and `evals_tab_case_form_shows_expectation_beside_last_output` (both `client/src/app/agents/[id]/_components/AgentEditor/_components/EvalsTab/EvalsTab.test.tsx`). Confirmed via `pnpm test:unit`: 191/193 pass, exactly these two red, nothing else. Left for `test-writer` to tighten (per the fix-mode task) — e.g. assert the row's copy without opening it, and disambiguate the panel's copy with `within()`/`getAllByText` once opened, rather than a bare `getByText`.
- _2026-08-22_ — **Fixed the SPEC-04 fix-mode Gap 1 ("a returned finding is rendered without its title") by rendering `EvalReturnedFinding.title` and `.severity` in `EvalsTab.tsx`'s "Findings returned in the most recent result" panel, via a new `displayOrAbsent()` helper that renders an em dash + `aria-label="not available"` for a null/undefined value — never an empty string, never an invented default.** Deliberately did NOT add `category`: `EvalReturnedFinding` (`client/src/vendor/shared/contracts/eval-ci.ts:266-274`, mirrored server-side by `toReturnedFinding` in `server/src/modules/eval/runner.ts:64-72`) has no `category` field — only `file`/`start_line`/`end_line`/`grounded`/`severity`/`title`. A future ask to show a returned finding's category needs a contract change (both vendor copies + `runner.ts`) first, not just a client render fix — the case's OWN category (captured at creation, a different field on `EvalCaseRecord`) already renders in the row's badge and is not what this panel is about. Both new leaf spans (`s.fragmentTitle`, `s.fragmentSeverity`) sit alongside the pre-existing `s.fragmentMeta` span (file:line · grounded, text unchanged) inside a per-finding `s.fragmentItem` div — confirmed via `getNodeText`'s "direct text-node children only" rule that this restructuring doesn't disturb the existing regex-substring test matches (`/src\/config\.ts:10-12/`, `/\bgrounded\b/`) scoped by `within(resultsHeading.parentElement!)`.
- _2026-08-17_ — **Resolved the `ContextAttachPanel` checkbox-focusability defect (2026-08-17 Open Question below) by giving the checkbox `<span>` its OWN `tabIndex={0}` and leaving the row `<div>`'s `tabIndex={0}` in place, rather than moving the Tab stop off the row entirely** — the plan (`docs/plans/2026-08-17-context-attach-row-role-a11y.md`) offered two shapes: single-tab-stop-per-row (move `tabIndex` from the row onto the checkbox, repoint `rowRefs`) or this minimal one. The single-tab-stop option looks cleaner but breaks `getRow()` in `ContextAttachPanel.test.tsx` (`screen.getByText(path).closest('[tabindex="0"]')`) for every test that focuses "the row" (`moves a focused row…`, `does not permit reordering…`, `marks an attachment absent…`) — the text span is a sibling of the checkbox, not a descendant, so `.closest()` finds nothing once the row itself loses its `tabIndex`. Since `rowRefs`-based focus retention after `Alt+Arrow` also targets the row, keeping the row's own `tabIndex` was required to avoid touching test selectors that belong to `test-writer`. Net: two Tab stops per row on the state toggle (row, then checkbox) instead of one — a real a11y regression on tab-surface size versus the "recommended" option, accepted here to keep the reorder path and its existing tests untouched. If this component's tests are ever retargeted to query controls purely by role (no `getRow()`/`.closest('[tabindex="0"]')` selector), revisit and switch to the single-tab-stop shape.
- _2026-08-16_ — **Fixed `useSetOwnerContext`'s attach-side `token_count: 0` placeholder (see Open Questions 2026-08-16) by reading the real count from the documents-listing cache** — `hooks/context.ts`'s `onMutate` now does `qc.getQueryData<ContextListing>(["context", "documents", repoId])` and builds a `path → token_count` map from it; any `path` missing from `prev.attachments` (i.e. just toggled ON) now looks up its real count there instead of defaulting to `0`, falling back to `0` only if the listing cache itself has no entry (e.g. not yet fetched). `ContextAttachPanel.test.tsx`'s combined-count test (previously red on the attach half) is now green; detach/reorder behavior (which already worked) is unchanged since those paths still come from `prev.attachments` via `byPath`.

## What Doesn't Work

- _2026-07-30_ — **Portaling a hover-popup to `<body>` (to escape an ancestor's `overflow: hidden`) breaks the usual "parent `onMouseEnter`/`onMouseLeave`" hover pattern** — once the popup is `createPortal`'d out to `document.body`, it's no longer a DOM descendant of the trigger, so an instant `setHovering(false)` on the trigger's `onMouseLeave` unmounts the popup before the mouse has actually reached it (the browser's leave-before-enter event ordering means the popup can vanish mid-transition, with no element left to catch its `mouseenter`). Adding matching `onMouseEnter`/`onMouseLeave` to the popup itself is necessary but NOT sufficient. Fix: debounce the close (`setTimeout(~120ms)` in the leave handler) and cancel it from either element's `onMouseEnter` — see `SeverityCounts.tsx`. Any future portaled hover-UI in this app needs the same debounce, not just mirrored handlers.
- _2026-07-30_ — **When a server aggregation's SEMANTICS change, grep the client for every independent re-derivation of the same metric** — changed the PR-list route's findings/cost aggregation from "latest review overall" to "latest review per agent, summed" (see server `insights.md`), but two client-side spots independently re-derived "the findings for this PR" from raw review data: `PRRow`'s hover-popup content and `FindingsTab`'s page-level aggregate counter. Both still used the OLD single-latest-review logic, so the popup showed "No findings" while the (now-correct) badge count showed real numbers — a real user-visible bug from touching only the server side of a semantic change. Fixed by extracting one shared `latestReviewPerAgent()` helper (`pulls/helpers.ts`) and using it in both places instead of leaving each to reimplement the same "which review(s) count" logic.
- _2026-07-30_ — **`?? 0` before summing two nullable numeric inputs hides "missing" as "zero"** — `RunCostBadge`'s detailed variant (`src/components/run-cost/RunCostBadge.tsx`) computed `(tokensIn ?? 0) + (tokensOut ?? 0)` and displayed "0 tok" for runs with no recorded token data, indistinguishable from a run that genuinely used zero tokens. Fix: check both inputs for `null`/`undefined` explicitly (`tokensIn != null && tokensOut != null ? tokensIn + tokensOut : null`) and render a placeholder (`"—"`) when either is absent, rather than defaulting to 0 before the null-check. Applies to any display code combining nullable numeric fields — `?? 0` is fine for arithmetic accumulators that get used further, wrong for anything rendered straight to the user.

## Codebase Patterns

- _2026-08-22_ — **Superseded by `implementer`'s fix, same day: `EvalsTab`'s collapsed case row now DOES render the expectation type (a `<p>` in `s.rowInfo`, always visible, not gated by `open`), closing the gap the entry right below this one flagged.** The expanded panel keeps its OWN "Expected outcome" block too (AC-55 needs the expectation beside the returned findings there), so the identical sentence now legitimately renders TWICE once a row is opened — `getByText`/`queryByText` for that sentence throws "multiple elements" the moment a test opens the row after also asserting the collapsed copy. Fix in `EvalsTab.test.tsx`: assert the collapsed row's copy with a plain (unscoped) `getByText` BEFORE opening the row — that now IS the real AC-52 assertion, no interaction needed — then, after opening, scope the panel's copy with `within(screen.getByText("Expected outcome").parentElement!).getByText(...)`. Do not disambiguate with `getAllByText(...)[n]` — an index survives the two copies swapping DOM position, which defeats the point of a test whose job is to pin WHERE each copy renders, not merely that it exists twice.
- _2026-08-22_ — **Also superseded by the same fix: a returned finding's panel entry now renders `title` and `severity` beside the file:line/grounding text** (`displayOrAbsent(f.title)`/`displayOrAbsent(f.severity)`, each with its own `aria-label` "not available" when absent) — the entry below, about a title-less `file:line · grounded/ungrounded` line, described the pre-fix state. The substring-based assertions the entry recommended (`/src\/foo\.ts:1-2/`, `/\bgrounded\b/`, scoped with `within()`) needed NO change to survive this — which is the payoff of asserting substrings/regex instead of one exact composed string when a line is known to be growing more fields. One correction: `EvalReturnedFinding` carries no `category` field at all (only `file`, line range, `grounded`, `severity`, `title`) — don't invent one in a fixture or assertion for this shape.
- _2026-08-22_ — **`EvalsTab`'s collapsed case row does NOT render the expectation type (`must_find`/`must_not_flag`) as visible text — only the opened case-form panel does** — SPEC-04's AC-52 lists "the expectation type" among what a case row must state, alongside pass/fail and severity/category, but `EvalsTab.tsx`'s `CaseRow` only reads `caseExpectation(c)` inside the `{open && …}` panel (the `t(\`caseModal.kind.${expectation.kind}\`)` line), never in the always-visible header. `EvalsTab.test.tsx > evals_tab_case_row_states_pass_kind_severity_and_category` asserts the kind text only AFTER clicking the row's Edit control to open it — treating "the row" as the collapsed header PLUS its expandable panel together (the file's own top comment cites AC-52 as covered by "each case's own pass/fail/never-run state, expectation and expected/returned finding counts" without distinguishing collapsed vs. open). If a future AC or design review wants the expectation type visible WITHOUT opening the row, that's a real gap to close in `EvalsTab.tsx`, not a test to loosen further.
- _2026-08-22_ — **The eval case panel's "Findings returned in the most recent result" list renders each finding as `file:line · grounded/ungrounded` with no title** — `EvalsTab.tsx`'s `CaseRow` panel (`s.fragmentList` block) never reads `f.title` even though `EvalReturnedFinding.title` exists on the contract and is populated by the seed. Tests asserting this block (`EvalCaseRecord.latest_result.findings`) should assert the file/line-range and grounding state as separate substring matches (`/src\/foo\.ts:1-2/`, `/\bgrounded\b/`), scoped with `within()` to the results section (the row's own location line duplicates the same file:line text) — NOT one exact composed string — since a title is expected to be added to this line later and a test pinning the current title-less composition would need an unrelated edit the moment it lands.
- _2026-08-22_ — **`IconBtn` (`vendor/ui/primitives/IconBtn.tsx`) has no `disabled` prop** — only `icon`, `label`, `size`, `active`, `onClick`, `danger`. A row action that needs to stop responding mid-mutation (e.g. a per-case "verify" icon while its own `useMutation` is `isPending`) can't just pass `disabled` — it silently does nothing (not a TS error, since `rest` isn't spread onto the `<button>`; extra props are simply dropped). Either guard inside `onClick` (`if (pending) return;`) or use a real `<Button icon=...>` (which DOES forward `disabled`) instead, for any control that needs a pending-disabled state. `EvalsTab`'s per-case row verify icon (SPEC-04) accepted the gap deliberately — the row's own `aria-live` status region and the case-form panel's full `Button` (which does disable) cover the requirement instead.
- _2026-08-22_ — **When a plan promotes a shared `helpers.ts` out of one component's folder (e.g. `RunHistoryTable/helpers.ts` → `components/eval-runs/helpers.ts`, SPEC-04 D16) and only names TWO importers to repoint, grep the whole tree for the OLD import path before deleting the file** — `RunCompareDialog.tsx` and `RunCompareDialog/helpers.ts` both imported `DisplayValue`/`formatMetricPercent`/`formatRunCost` from `../RunHistoryTable/helpers` too, and neither was in the plan's D16 file list (only `RunHistoryTable.tsx` and `EvalDashboardView.tsx` were named). Deleting the old file without also repointing those two would have been a silent production TypeScript break outside the plan's declared scope. `grep -rn "RunHistoryTable/helpers"` across `client/src` before deleting is the check; the fix (two one-line import path edits, no test files touched) is small enough to fold into the same task rather than stopping to ask.
- _2026-08-21_ — **`RunHistoryTable`'s row-selection checkbox never lets a user reach three selections — it disables every OTHER unselected checkbox once two are already checked** (`disabled={!selected.includes(r.id) && selected.length >= 2}`), so AC-30 ("compare needs exactly two") can never be driven to a literal "three selected" UI state to test against — `fireEvent.click()` on the disabled third checkbox is a no-op (no `onChange` fires), leaving selection at two and Compare still enabled. `RunHistoryTable.test.tsx`'s `run_history_compare_needs_exactly_two` test therefore asserts the invariant that actually holds: 0/1 selected → Compare disabled, exactly 2 → enabled, and the third row's checkbox is itself `disabled` (plus a no-op click leaves the first two checked, the third unchecked, Compare still enabled) — not "select three, see Compare go back to disabled", which this component structurally prevents.
- _2026-08-21_ — **`Modal` (`vendor/ui/kit/Modal.tsx`) renders TWO elements with the identical accessible name "Close" whenever a caller supplies both `onClose` (header X, an `IconBtn` with `aria-label="Close"`) and its own footer Close button (`RunCompareDialog`'s footer `<Button>{t("compare.close")}</Button>` where the copy is literally "Close")** — `screen.getByRole("button", { name: "Close" })` throws "multiple elements". `RunCompareDialog.test.tsx`'s focus-trap test uses `getAllByRole("button", { name: "Close" })` and relies on DOM order (`[0]` = header X, always first in `Modal`'s JSX before `children`; `[1]` = the caller's own footer button) to tell them apart — which is also exactly the order `use-focus-trap.ts`'s `FOCUSABLE_SELECTOR` walk finds them in, so this doubles as the natural way to assert "first focusable" / "last focusable" for the Tab-wrap test. Any future test of a `Modal` consumer whose own footer copy happens to also read "Close" needs the same `getAllByRole` + index approach, not a unique-name query.
- _2026-08-21_ — **A nav item's display label is duplicated in TWO places, and only one is `NAV` itself** — `vendor/ui/nav.ts`'s `NavItemDef.label` feeds the sidebar (`Sidebar.tsx`/`NavItem.tsx` render `item.label` directly), but `app-shell/hooks/useShellCommands.ts` builds the command-palette entry from `t(\`nav.${it.key}\`)` against the **`shell`** namespace (`messages/en/shell.json`'s `nav.*` block) — it never reads `NavItemDef.label` at all. Adding the Eval Dashboard nav item (SPEC-03 T22) needed no `shell.json` edit only because that key (`nav.eval: "Eval Dashboard"`) was already pre-built scaffolding (root `CLAUDE.md`'s "course starter" convention — same shape as the `smartDiff.*`/`rollupSeverities` precedents already logged here). A future nav item that ISN'T pre-seeded in `shell.json` will render fine in the sidebar (no missing-translation error, since `NavItem` doesn't call `t()`) but will throw or fall back ugly in the command palette specifically — check `messages/en/shell.json`'s `nav.*` block, not just `NAV`'s own `.label`, before assuming a new nav entry is fully wired.
- _2026-08-21_ — **`Modal` (`vendor/ui/kit/Modal.tsx`) doesn't forward a ref, so a caller that needs the ACTUAL rendered `role="dialog"` node (for a focus trap) can't get it by wrapping `<Modal>` — the fix is to ref an element INSIDE `Modal`'s `children` and walk up with `.closest('[role="dialog"]')`** — `RunCompareDialog`'s `use-focus-trap.ts` attaches its `anchorRef` to the same `padding: 24` wrapper div every `Modal` caller already needs (`client/insights.md`, 2026-08-04 entry below), then resolves `anchorRef.current!.closest('[role="dialog"]')` inside a `useEffect` to reach `Modal`'s own dialog root — which correctly includes `Modal`'s own header Close (X) button in the trapped Tab order, not just the caller's own content. Any future per-caller a11y addition to a `Modal` consumer (focus trap, `aria-describedby` wiring, etc.) that needs the dialog's real DOM node should use this same `.closest()` pattern rather than trying to thread a ref through `Modal`'s props (which don't exist for this).
- _2026-08-17_ — **A hand-built `RunTrace` object literal in a client test fixture breaks at RUNTIME (not just typecheck) the moment a new required field is added to the shared `RunTrace` Zod contract, because `RunTrace.parse()` is never called client-side** — `RunTraceDrawer.test.tsx`'s `baseTrace` fixture was missing `specs_excluded` after `contracts/trace.ts` added `specs_excluded: z.array(ContextExclusion).default([])` (`docs/plans/2026-08-16-project-context-run-injection.md`, U1/U2). Client `pnpm typecheck` catches this (TS2741, since `.default()` makes the field required in `z.infer`'s output type — same rule already logged in `server/insights.md` 2026-07-30) but the WORSE symptom is `pnpm test:unit`: `TraceBody.tsx` reads `trace.specs_excluded.length` unconditionally (mirroring the existing `specs_read.length` Row), so the fixture's `undefined` throws `Cannot read properties of undefined (reading 'length')` at render time, not a clean assertion failure. Any future field added to `RunTrace`/`RunStats`/`PromptAssembly` needs the SAME fixture update in `RunTraceDrawer.test.tsx` (and any other hand-built trace fixture) — grep the client for `specs_read:` or the contract's other required keys to find every literal fixture before assuming the shared-contract change is "just a server thing".

- _2026-08-17_ — **ARIA's presentational-children rule (children of `role="checkbox"` are permitted to be hidden from AT entirely) means a composite row that needs BOTH a checked-state indicator AND independently focusable controls (a preview button, a reorder handle) cannot put `role="checkbox"` on the row itself if those controls live inside it** — `ContextAttachPanel.tsx`'s row previously put `role="checkbox"`/`aria-checked`/the `attachAria` name on the outer row `<div>`, with a real `<button>` (preview) and a `role="img"` grip nested inside; per the rule, AT could legally expose neither. Fixed by moving `role="checkbox"`/`aria-checked`/`attachAria` down onto the small leaf indicator span (the visual checkmark box, whose only child is a decorative icon) instead of the row — a leaf has no interactive descendants, so the rule no longer applies, and the row stays a plain focusable container that still owns the actual toggle (`onClick`/Enter-Space) and reorder (`Alt+Arrow`) interactions via its own handlers. Any future "row represents one togglable state but also contains other actionable children" component in this app needs the same split: role/state/name on the leaf state-indicator, interaction handlers stay on the row.
- _2026-08-17_ — **A drag-handle promoted from `role="img"` to a real `<button>` nested inside a row that owns `Alt+Arrow` reorder keydown handling does not need its own reorder logic — the keydown bubbles from the button up to the row's existing handler for free** — `ContextAttachPanel.tsx`'s grip button has no `onKeyDown` of its own; React attaches the row's `onKeyDown` via normal DOM bubbling, so `Alt+ArrowUp/Down` fired while the grip button has focus still reaches the row's handler unchanged. The button's `onClick` only needs `e.stopPropagation()` (to stop the row's own `onClick` from toggling attach when the grip is clicked) and a `disabled={!draggable}` to match `AC-14`'s "no reorder while filtering" gate — no direction-specific click behavior was needed to satisfy "the grip performs the same move as `Alt+Arrow`".
- _2026-08-16_ — **`VALID_TABS` in a sibling list-view's constants.ts silently swallows a new detail-pane tab key added elsewhere** — adding `"context"` to `SkillDetail/constants.ts`'s `TABS` was not enough on its own: `SkillsView.tsx` validates `?tab=` against a SEPARATE `VALID_TABS` array in `SkillsView/constants.ts` and falls back to `"config"` for anything not in it, so `?tab=context` would silently reset on reload/deep-link without also adding `"context"` to that array. The two lists (`SkillDetail`'s `TABS` for rendering, `SkillsView`'s `VALID_TABS` for URL validation) are not derived from each other — check both when adding a detail-pane tab.
- _2026-08-16_ — **A tab whose `labelKey` needs to resolve in a DIFFERENT next-intl namespace than the rest of its tab list can't use the single shared `t(tb.labelKey)` call** — `SkillDetail.tsx`'s `TABS` (config/context/preview/versions) share one `useTranslations("skills")` call, but the new Context tab's copy lives in `messages/en/context.json` (shared with the agent editor's identical tab, and with `ContextAttachPanel` itself) rather than duplicated into `skills.json`. Fixed by adding a second `const tc = useTranslations("context")` and special-casing `tb.key === "context"` in the `tabs.map()` that builds the `<Tabs>` prop, instead of making `labelKey` resolution namespace-aware generically. Any future cross-namespace tab entry in an existing `TABS` array needs the same one-off special case.
- _2026-08-08_ — **`MonoLink` (`vendor/ui/primitives/MonoLink.tsx`) renders an inert, focusable `<button onClick={undefined}>` — not plain text — whenever `href` is falsy, and this is a separate gap from the `title`-prop one logged just below** — `MonoLink.tsx:47-58`'s fallthrough always returns a `<button>`, so a caller that conditionally omits `href` (e.g. because `repoFullName`/`headSha` aren't resolved yet) gets a dead, hoverable/focusable control unless it branches around `MonoLink` itself. Blast-tab's line-level references (`specs/0007-blast-radius-ux-fixes.md` §0) needed a real text fallback in that case, so a small local wrapper (`LineRef` in `BlastTab.tsx`, not promoted anywhere — single consumer) checks `href` first and renders a plain `<span>` when it's absent, falling through to `<MonoLink href={href}>` only when a link is possible. Any future consumer of `MonoLink` with an optional/nullable `href` needs the same guard — do not assume "no href" degrades gracefully to text on its own.
- _2026-08-08_ — **`MonoLink` (`vendor/ui/primitives/MonoLink.tsx`) had no `title` prop before the Blast-tab in-diff-jump feature (`specs/0006-blast-radius-polish.md` §3a), even though the design decision calling for it (a `title`/`aria-label` distinguishing the same-page-navigation button from the external-link affordance, §5) assumed one existed** — added `title?: string`, forwarded to both the `<a>` and `<button>` branches, as a small backward-compatible extension (optional prop, no behavior change for any existing caller). Any future plan/spec that shows example JSX passing a prop to a vendored `@devdigest/ui` primitive should be treated as "verify the prop exists first" — `MonoLink`'s actual shape at the time didn't match the plan's snippet, and the fix was additive rather than a sign the plan was wrong.
- _2026-08-05_ — **A per-item deep-link into a tab that gets unmounted must have its target state owned by an ancestor that survives the tab switch — this is now a two-time pattern** — the existing run-level Timeline→accordion scroll (`targetRunId`/`targetNonce`) lives inside `FindingsTab.tsx` because both the trigger (`RunHistory`) and the destination (`ReviewRunAccordion`) are mounted together there. The Smart Diff finding-marker → Agent-runs-tab navigation (L03) could NOT follow that shape: the trigger (`DiffTab`, on the Files-changed tab) and the destination (`FindingsTab`, on the Agent-runs tab) are DIFFERENT tabs, and `page.tsx` only mounts one tab's content at a time (`tab === "findings" && <FindingsTab/>`). Putting the target state inside `FindingsTab` would lose it the instant the tab switched and unmounted the setter. Fixed by owning `findingTarget` in `page.tsx` itself — the one component that stays mounted across a `?tab=` change on the same route (`page.tsx` → `FindingsTab` → `ReviewRunAccordion` → `FindingsPanel`, mirroring `targetRunId` one level deeper). Any future CROSS-TAB (not just cross-component) deep-link in this app needs the same placement: state on the nearest ancestor that outlives BOTH the trigger and the destination, not just the destination.
- _2026-08-05_ — **A third confirmed instance of "pre-built, unused i18n scaffolding waiting for its lesson"** — `messages/en/prReview.json`'s `smartDiff.*` block (`coreLabel`/`wiringLabel`/`boilerplateLabel`/`largeTitle`/`largeBody`/`filesCount`/`findingLines`/`groupedByRole`) existed with zero consumers before the Smart Diff feature (L03) was implemented, the same pattern already logged for `pulls/status.ts`'s `rollupSeverities` and the `reviews` module's pre-existing `upsertIntent`/`getIntent` pair (`server/insights.md`, 2026-07-30 / 2026-08-05). Reused as-is; only `findingLines`'s English value needed to change (`"{count} finding-lines"` → `"{count} findings"`). Before adding new copy for a course-lesson feature, grep the relevant `messages/en/*.json` file for a plausible key first — it is often already there, pre-built ahead of the lesson that consumes it.
- _2026-08-05_ — **`client/src/vendor/shared/adapters.ts` is NOT a byte-for-byte mirror of `server/src/vendor/shared/adapters.ts`** — the client's copy is missing several members the server's has (e.g. `GitClient.sync`/`diffNameOnly`, `GitHubClient.commitFiles`/`findOpenPr`, `StructuredRequest.sessionId`), presumably because the client only ever needed a subset historically. When adding a NEW adapter interface (e.g. `WebFetchClient` for L03) that both copies must carry, diff just the new block you're adding between the two files — don't assume "keep the copies identical" means the whole file is otherwise in sync, it isn't.
- _2026-08-04_ — **A mocked `next/navigation` router that mutates a module-level variable does NOT trigger a React re-render on its own** — `SkillsView.test.tsx` (and the earlier version of it) mocks `useRouter().replace` to write into a module-level `URLSearchParams`, and `useSearchParams()` reads that same variable. Firing a click that calls `replace()` updates the variable immediately, but the component that already rendered has no way to know — React only re-renders from state/props/context changes, and a plain module variable is none of those. Symptom: `waitFor(() => expect(screen.getByText(...)).toBeInTheDocument())` times out even though the assertion "should" eventually be true, because nothing ever re-renders to make it true. Fix: after any action that depends on the mocked `replace()` taking effect, explicitly force a fresh render (`cleanup(); renderView();` — see `renderAfterNavigation()`) rather than waiting. This is not a new problem introduced by the Skills work — the original `SkillsView.test.tsx` already worked around it for its one selection test; the redesign just added several more assertions (tab switching) that needed the same treatment.
- _2026-08-04_ — **A drag-reorder list needs an optimistic mutation, and `cancelQueries` is the non-obvious half of it** — `useSetAgentSkills` (`src/lib/hooks/agents.ts`) is the only optimistic hook in the app; every other mutation invalidates on success. Without `onMutate` a dragged row snaps back to the server order for a whole round-trip, which reads as "the drag didn't work". The part that is easy to omit: `await qc.cancelQueries({ queryKey: [...] })` FIRST — an already-in-flight GET that resolves after the optimistic `setQueryData` overwrites it with the pre-drag order, producing an intermittent revert that looks like a server bug. `SkillsTab.test.tsx` pins this by holding the POST promise open and asserting the new row order renders while it is still pending.
- _2026-08-04_ — **Reordering from a filtered list silently drops the hidden items** — the agent Skills tab persists the FULL ordered id array (`POST /agents/:id/skills { skill_ids }`), so a drag performed while a filter hides some linked skills would write only the visible subset and detach the rest. Dragging is disabled outright while the filter box is non-empty (`SkillsTab.tsx`, with a hint swap) rather than trying to splice the visible subsequence back into the full list. Any future "reorder a filtered list" UI in this app has the same trap.
- _2026-07-30_ — **A component shared between the PR-list route and the PR-detail (`[number]`) route lives under `pulls/_components/`, not `@devdigest/ui`** — `SeverityCounts` (severity readout used by both `PRRow` in the list and `RunHistory`/`FindingsTab` on the detail page) is colocated at `pulls/_components/SeverityCounts/`, imported from the detail side via a relative path up through `[number]/_components/` (e.g. `../../../_components/SeverityCounts` from `pulls/[number]/_components/RunHistory/`). Kept out of `@devdigest/ui` deliberately since it's specific to the findings/severity domain, not a generic UI primitive — promote it there only if a third, unrelated consumer shows up.
- _2026-07-30_ — **A TanStack Query hook can be made "fetch only while hovered" by adding an `{ enabled }` option, reusing the SAME hook/endpoint another page already calls** — `usePrReviews(prId, { enabled })` (`src/lib/hooks/reviews.ts`) is the same hook the PR-detail page already uses for its full findings list; `PRRow` on the PR-list page calls it too, gated on `enabled: popupHover`, to lazily fetch one row's findings only when its FINDINGS-column popup is actually hovered — no separate lazy-fetch hook needed, and react-query's cache makes re-hovering the same row instant after the first fetch.

## Tool & Library Notes

- _2026-08-08_ — **`@testing-library/user-event` is NOT an installed dependency in `client/`** — it's absent from `package.json` and `node_modules/@testing-library/` (only `react` and `jest-dom` are present), despite the `react-testing-library` skill's default guidance to always use it over `fireEvent`. `DiffTab.test.tsx`'s pre-existing click test already uses `fireEvent.click(...)`, which is this codebase's actual (if not skill-preferred) convention. When writing a NEW interaction test in `client/`, check `node_modules/@testing-library/user-event` exists before importing it — if it doesn't, use `fireEvent` to match the rest of the suite rather than silently adding a new dependency (`pnpm add -D`) as a drive-by inside an unrelated feature change.
- _2026-08-04_ — **react-markdown v9's `code` component override fires for BOTH inline code and fenced-block code with no `inline` prop to distinguish them (that prop was removed after v6), and an inline `style` attribute can never be beaten by a CSS rule, no matter the selector** — `Markdown.tsx`'s single `code` override applied inline-chip styling (small `border-radius`, `background`, `padding: "1px 6px"`) unconditionally to every `<code>`; `styles.css`'s `.dd-md pre code { background: none; padding: 0 }` was written to cancel that out for fenced blocks but structurally can't, since inline `style=""` always wins over any class selector regardless of specificity. Symptom: a fenced code block rendered as two mismatched nested rounded boxes (the block's own `.dd-md pre` background+radius, plus the chip's smaller one inside it) — reads visually as "broken"/cut-off corners. Fix: override `pre` separately in the `components` map, extract the already-rendered `code` child's raw text (`children.props.children`), and re-render a plain `<pre><code>{text}</code></pre>` with NO inline styles, letting `.dd-md pre` / `.dd-md pre code` (CSS) own the block's look entirely — react-markdown only routes a fenced block's `code` node through the separate `pre` override once one exists, so the original `code` override becomes reachable only for genuine inline code.
- _2026-08-04_ — **Customizing `::-webkit-scrollbar-thumb`/`-track` without also setting `::-webkit-scrollbar-corner` leaves a stray white/light square wherever an element scrolls in BOTH directions at once** — `SkillBodyEditor`'s textarea (`overflowX: auto; overflowY: auto`) shows a horizontal scrollbar on long unwrapped lines; the corner square where it meets the vertical scrollbar defaulted to the browser's own light color since no rule targeted it, reading as a stray white triangle against the dark theme. Fixed globally in `vendor/ui/styles.css`, right next to the existing thumb/track rules, rather than per-component — any future element that scrolls both ways inherits the fix automatically.
- _2026-08-04_ — **This sandbox's Browser pane does not composite frames, so `document.body.innerText` (and the `get_page_text`/screenshot tools built on rendered layout) can report EMPTY or stale content even though the real DOM is fully populated** — `innerText` requires layout to have run; `textContent` does not, and reliably reflects the live DOM regardless of compositing state. When verifying a client change in this environment, read `document.querySelector(...).textContent` (or `.innerHTML`) via `javascript_tool` instead of `get_page_text` / `computer screenshot`, and don't treat an apparently-blank page as evidence of an app bug without checking `textContent` first. Separately: repeatedly stopping/restarting the dev server while reusing the same browser tab across many HMR cycles in one long session can leave the page stuck showing loading skeletons forever with no console error — confirmed reproducible on an untouched, pre-existing route too, so it's tooling/session churn, not app code. A full server restart AND a brand-new tab (`tabs_create`) together resolve it; either alone did not.
- _2026-08-04_ — **Tailwind's Preflight (`@import "tailwindcss"`, `styles.css:1`) combines with this repo's OWN `h1,h2,h3,h4,p { margin: 0 }` reset (`styles.css:205-211`) to make ALL rendered markdown look like flat, unstyled body text app-wide** — Preflight already strips heading font-size/weight and list `list-style`/`padding-left` to zero; the repo's reset then also zeroes heading/paragraph margins on top. `Markdown.tsx` (`vendor/ui/primitives/Markdown.tsx`) only overrides `p`/`strong`/`code`/`a` and emits `className="dd-md"` on its wrapper, but **no `.dd-md` CSS rule existed anywhere** — so every consumer (skill body preview, import preview, anywhere `<Markdown>` is used) rendered `## Heading` and `- item` as plain paragraphs with no bullet. Not a bug in any one feature; it's a hole in the shared primitive that every future consumer inherits silently. Fixed by adding a `.dd-md` block to `vendor/ui/styles.css` that explicitly RE-establishes heading sizes/weights/margins and `ul`/`ol` `list-style`+`padding-left` (plus `li>p{margin:0}`, since react-markdown wraps loose list items in `<p>` which would otherwise inherit the 10px bottom margin from `Markdown.tsx`'s own `p` override and blow list spacing apart). Before touching `Markdown.tsx`'s `components` overrides for a NEW element type, check whether `.dd-md` in `styles.css` already needs it instead — CSS is the right layer for anything that isn't per-element JS logic (first-child margin, nested list spacing).
- _2026-08-04_ — **Hand-rolled HTML5 drag-and-drop has three failure modes that all look like "drag is broken"** (`src/lib/drag-list.ts`, written this way because the app carries no DnD dependency): `onDragOver` MUST call `preventDefault()` or the drop event never fires at all; `dataTransfer.setData(...)` must be called in `onDragStart` or Firefox refuses to begin the drag; and `dataTransfer.getData()` is unreadable during `dragover` in most browsers, so the dragged id has to be tracked in component state rather than round-tripped through the event. In jsdom tests, `fireEvent.dragStart/dragOver/drop` do not synthesise a `dataTransfer` at all — pass a stub (`{ setData, getData, effectAllowed, dropEffect }`) in the event init or the handlers throw.
- _2026-07-30_ — **Setting only `overflowY: "auto"` on a fixed-width box lets unbreakable text force a horizontal scrollbar** — per the CSS overflow spec, when one axis is set to something other than `visible` and the other is left at the default `visible`, the `visible` one computes to `auto` too. `SeverityCounts`' popup (`overflowY: "auto"`, `maxWidth: 360`, no explicit `overflowX`) grew a horizontal scrollbar whenever a finding's file path (no spaces to wrap on) was long. Fix: set `overflowX: "hidden"` explicitly as a hard backstop, AND add `overflowWrap: "anywhere"` / `wordBreak: "break-word"` / `flexWrap: "wrap"` to the row holding the path so it actually wraps instead of being invisibly clipped. Overflow-hidden alone would silently cut the text off — both halves of the fix are needed.

- _2026-08-18_ — **`MonoLink`'s `title` prop is NOT usable to give a file-reference button an accessible name that differs from its visible text** — `title` only enters the accessible-name computation as a last resort, after element content; since `MonoLink`'s button always has visible `children` (the path text), `title` is silently ignored by both browsers' accname algorithm and testing-library's `getByRole(..., {name})`. SPEC-02's `RiskRow`/`ReviewFocus` file references need a name like "Go to src/foo.ts in Files changed" while showing only the short path as visible text — `MonoLink` can't do this, so both components use a plain local `<button aria-label={...}>{path}</button>` instead (styled inline to match `MonoLink`'s look, not routed through it). Before reusing `MonoLink` for any control whose accessible name must differ from its rendered text, confirm this — it will need an `aria-label` prop added to the primitive first (not present as of this session).
- _2026-08-18_ — **A plan's task-level "→ ACs → test names" annotation is not the full list of what that task's AC requires** — `docs/plans/2026-08-17-pr-brief.md` T27 bound to AC-3/5/7/11/13 via test names, but AC-1 ("present … that brief's what statement, why statement and risk level") also applies to `BriefCard` and was easy to miss because none of T27's *listed* test names mention risk level (AC-1's own listed tests are all in `OverviewTab.test.tsx`/e2e, about DOM order, not content). Caught by reading the AC text in `docs/specs/cross/SPEC-02-pr-brief.md` directly rather than trusting the task's test-name shorthand alone. When a plan task's prose (not just its bound test list) mentions a data field, render it — the Traceability table's "which test proves it" mapping can undercount which task actually has to produce it.

## Recurring Errors & Fixes

- _2026-08-21_ — **Superseded by a fix to the residual half described in the Open Questions entry below (same date): a seeding `useEffect` keyed ONLY on a stable id (`draft?.finding_id`) never re-fires when a background refetch under that same id resolves with different data — the id staying constant is exactly the case that needs the re-seed.** `EvalCaseModal.tsx`'s effect now depends on the actual seed values (`draft?.default_expectation_kind`, `draft?.suggested_name`, `draft?.existing_case`) instead of just `finding_id`, so it fires again once the fresh draft lands — while a new `touchedRef` (set by wrapping `setKind`/`setName` in `selectKind`/`editName`) guards the seed so a deliberate in-flight user edit still isn't clobbered, which is what the original `finding_id`-only key was trying (incompletely) to protect against. General shape for future "seed local state from a query result once" effects: key the effect on the VALUES that should trigger a reseed, not on an id that's stable across the exact refetch you need to react to, and use a per-mount ref flag (not the dependency array) to express "don't clobber user input" instead.
- _2026-08-21_ — **The global `staleTime: 30_000` default (`lib/providers.tsx`) makes a `useQuery` whose key is stable across remounts serve up-to-30s-stale data on reopen, even when the component fully unmounts/remounts (e.g. a conditionally-rendered modal `{open && <Modal/>}`)** — `useEvalCaseDraft`'s key is `["finding", findingId, "eval-case-draft"]`, which does NOT change when the finding's own `accepted_at`/`dismissed_at` changes (a separate mutation, different query key). Reopening `EvalCaseModal` on the same finding within the 30s window remounted the query observer but, since the cached data wasn't yet stale, TanStack Query served the cache without refetching — so the pre-selected expectation type reflected whatever the decision was up to 30s ago, not the current one. Fixed by overriding `staleTime: 0` + `refetchOnMount: "always"` on that one query — appropriate ONLY for a query whose correctness depends on data that can change from outside its own key between mounts; don't apply this pattern by default; most of this app's queries are correctly served from the 30s cache.
- _2026-08-18_ — **A `useMutation().mutate(...)` call does not invoke its `mutationFn` synchronously — asserting on a mocked `api.post` immediately after `fireEvent.click()`, in the same tick, sees zero calls even though the click handler ran.** Writing `BriefCard.test.tsx`'s regenerate test, `fireEvent.click(button); expect(post).toHaveBeenCalledWith(...)` failed with `Number of calls: 0`, even though a debug `console.log` right after the click confirmed exactly one matching `<button aria-label>` existed and the click dispatched. TanStack Query's `mutate()` schedules the actual `mutationFn` call asynchronously (a microtask), so nothing has reached the mock by the next synchronous line. Fix: assert via `await screen.findByText(...)` (or `await waitFor(...)`) on something that only appears **after** the mutation resolves, and check the mock call args only after that await — never immediately after `fireEvent.click()` on a control wired to a mutation. `IntentCard.test.tsx`'s recalculate test hit the same shape and used the same fix.
- _2026-08-17_ — **A tab added to an editor's `TABS` list is unreachable until the route's own `?tab=` whitelist is updated too — two lists, two files, and only one of them is where you're working.** `/agents/[id]/page.tsx` carried a hand-written `VALID_TABS = ["config", "skills"]` guarding `search.get("tab")`, while the tab bar came from `_components/AgentEditor/constants.ts`'s `TABS`. Adding the Context tab to `TABS` (SPEC-01 plan 1, T18 — a task whose file list named only those two AgentEditor files) rendered the button, and clicking it *did* push `?tab=context` into the URL, but the guard rejected the value and fell back to `"config"` on the very next render — so the panel never mounted and the URL looked correct the whole time. The skills side of the same feature worked purely by file layout: `SkillsView/constants.ts` holds `VALID_TABS` and the type list in one file, so whoever edited it saw both. Fix is structural, not a test: `page.tsx` now derives `VALID_TABS` from `TABS`, so divergence is impossible. **Two consequences worth generalising** — (1) before adding an entry to any tab/route/nav/enum list, grep the whole route folder for a *second* literal listing the same keys; a `?tab=`/`?view=` whitelist is almost always a separate one. (2) A URL assertion is not a rendering assertion: `e2e/specs/11-agent-context-tab.flow.json` passes `wait --url tab=context` even against the broken build, and only the last step (`wait --text "Toggle to attach"`, copy that exists solely in `ContextAttachPanel`) actually fails — verified by reverting the fix and re-running the suite (10/11 → the Context flow red).
- _2026-08-17_ — **A `.default(...)` added to a persisted Zod contract does nothing unless the read path actually calls `.parse()` — and adding the key to the test fixtures to fix the resulting type error hides that it doesn't.** `RunTrace.specs_excluded` was given `z.array(...).default([])` precisely so `run_traces` rows written before the field would read back as `[]`, but `server/.../run.repo.ts`'s `getRunTrace` returned `row.trace as RunTrace` and `useRunTrace` casts the JSON too — nothing between Postgres and `TraceBody` ever parsed, so the default never materialised and `trace.specs_excluded.length` threw on every pre-existing trace. The type error that `.default()` produces (it makes the field **required** in `z.infer`'s output) surfaced only in hand-built fixtures, and supplying the key there made the compiler happy while leaving every row already on disk broken. When a persisted contract gains a field: find the read path and confirm it parses, and write the regression fixture as the payload *without* the key (`const { specs_excluded: _drop, ...legacy } = TRACE`), because a fixture that supplies it cannot fail. in a test render-helper, because `??` treats `null` and `undefined` identically** — `BlastTab.test.tsx`'s `renderTab({ repoFullName: null })` was meant to force the "unknown repo" case, but the helper computed `overrides.repoFullName ?? REPO_FULL_NAME`; since the passed-in `null` is nullish, `??` fell through to the non-null default, so the "no link when `repoFullName` is null" test actually rendered WITH a real `repoFullName` and asserted on a link that shouldn't have existed (only caught because the assertion failed loudly, not silently). Any test helper whose whole point is to let a caller override a value to `null` must use default-PARAMETER destructuring (`{ repoFullName = DEFAULT }: { repoFullName?: X | null }`) instead of `??` inside the body — parameter defaults only fire on `undefined`, never on an explicitly-passed `null`, which is exactly the distinction needed here.
- _2026-08-07_ — **A grep-based "no raw `fetch(` outside the hook" static guard has a false-positive on TanStack Query's own `refetch()`** — `grep -rn "fetch(" <folder>` (used in `specs/0005-blast-radius.md`'s verification steps and worth reusing for any future feature folder) matches the substring `fetch(` inside `refetch(`, which is the query result's own reload callback, not a network call. `BlastTab.tsx` destructures it as `{ refetch: reload }` specifically so the guard greps clean — do the same rename in any component that both (a) lives under a folder covered by this exact grep pattern and (b) calls a query's `refetch()` for a retry button, rather than leaving the false positive for someone to puzzle over later.
- _2026-08-07_ — **`borderColor` is itself a shorthand — pairing it with `borderLeftColor` in one inline-style object trips React's "conflicting property" warning, even though `border` was already avoided** — `FindingCard/styles.ts` carried a comment reading "never mix `border` shorthand with `borderLeft`" and used `borderStyle`/`borderColor`/`borderWidth`/`borderLeftWidth`/`borderLeftColor`, which looks all-longhand but isn't: `borderColor` expands to the four per-side colors, one of which `borderLeftColor` then overrides. React only warns when the shorthand's VALUE CHANGES on a rerender, so this stayed invisible until something toggled it — here `focused`, flipping as a Smart Diff marker navigated to the card, which made it read as an intermittent navigation bug rather than a static style problem. Fix: write `borderTopColor`/`borderRightColor`/`borderBottomColor` explicitly and leave the left side to `borderLeftColor`. The same latent pair existed in `conventions/_components/ConventionCard/styles.ts` (keyed on `status`) and was fixed alongside. When a per-side override is needed, no `border*` group property is safe — only the true per-side longhands are.
- _2026-08-04_ — **`Modal` (`vendor/ui/kit/Modal.tsx`) applies ZERO padding to its children — every caller must supply its own padded wrapper, or content sits flush against the modal edges** — unlike `Drawer` (`vendor/ui/kit/Drawer.tsx`), which bakes `padding: 24` into its own children wrapper, `Modal`'s content div is only `{ flex: 1, overflow: "auto" }`, no padding at all. `CreateAgentModal` gets this right (`<div style={s.body}>`, `padding: 24`, its own `styles.ts`) but `CreateSkillModal` (pre-existing, `app/skills/_components/CreateSkillModal/`) and a new Conventions-merge-to-skill modal both originally shipped without the wrapper and rendered edge-to-edge with a "broken" look (reported independently twice — once per modal). Before adding any new `<Modal>` consumer, add a `padding: 24` wrapper div around its children by default — `Modal` will never do it for you.

- _2026-08-08_ — **A scroll-target `useEffect` living on the destination card itself is not a stable enough lifecycle when its ancestor swaps subtrees mid-load** — `FileCard`'s original blast-radius "jump to file" implementation (`specs/0006-blast-radius-polish.md` §3c) scheduled `scrollIntoView` from `FileCard`'s own effect (deferred two `requestAnimationFrame`s to let `setOpen(true)` paint first). It never fired: `DiffTab` renders the flat `DiffViewer` fallback while `usePrSmartDiff` is loading, then swaps to `SmartDiffViewer` once it resolves — a full subtree replacement, not a re-render — so the target `FileCard` mounts, its effect schedules the deferred scroll, and the swap unmounts it (running the cleanup, cancelling the scheduled frames) before they fire; this repeated across the swap's several remounts (confirmed live: 4 "effect fired"→"cleanup" pairs within ~250ms, zero of the two rAF callbacks ever ran). Fix: own the scroll from `DiffTab` instead — it never remounts across that swap, only its rendered output changes — via a self-terminating poll (`setTimeout`, capped attempts) that waits for `document.getElementById(targetId)` to exist with a non-zero rect; `FileCard`'s own effect now only force-opens (`setOpen(true)`), which is idempotent and safe to rerun on every one of its remounts. General lesson: a scroll/focus/animation effect that depends on "the target DOM node existing and being laid out" belongs on the nearest ancestor that is guaranteed not to unmount across the transition that produces that node, not on the node's own component.
- _2026-08-08_ — **`requestAnimationFrame` callbacks can silently never fire in this project's browser-automation testing tool** when its pane reports "not displayed, so the page is not compositing frames" (the same message `computer.screenshot` gives when the pane isn't visible) — confirmed by instrumenting a `useEffect`'s rAF chain: the effect body itself ran (logged), but neither the first nor a chained second `requestAnimationFrame` callback ever logged, even after several seconds. Rewriting the same wait as `setTimeout` fired immediately. Two implications: (1) prefer `setTimeout` over `requestAnimationFrame` for a "wait a tick then check the DOM" pattern in code that must also work headless/non-composited, not just for real foregrounded tabs; (2) **`Element.scrollIntoView({ behavior: "smooth" })` also silently never completes in a non-composited tab** (smooth-scroll is compositor-driven) — `{ behavior: "instant" }` applies immediately regardless. When verifying a `scrollIntoView`-based feature through this tool and the scroll position doesn't move even though your code logs that it called `scrollIntoView`, this is very likely why — don't conclude the feature is broken from that alone; substitute `instant` locally to confirm the target/position logic is correct, then trust `smooth` for real users (a real, foregrounded browser tab composites normally). Ship `smooth` when matching an existing convention (e.g. `FindingsPanel`'s own scroll-to-finding already uses it) — the non-completion is a property of this specific test harness, not of real browsers.

## Session Notes
<!-- written by a separate end-of-session wrap-up flow, not this skill -->

## Open Questions

- _2026-08-21_ — **The `useEvalCaseDraft` fix (`staleTime: 0` + `refetchOnMount: "always"`, see Recurring Errors & Fixes 2026-08-21 just below) makes the QUERY refetch fresh data on every `EvalCaseModal` reopen, but `EvalCaseModal.tsx`'s own effect that seeds the form's `kind`/`name` local state from the draft does NOT re-sync to that fresh data when the SAME finding is reopened with a warm cache — confirmed with a direct repro, test written and left red on purpose (`EvalCaseModal.test.tsx > eval_case_modal_reopen_reflects_a_decision_that_changed_while_closed`).** The seeding effect (`EvalCaseModal.tsx:29-35`) is `React.useEffect(() => { if (draft && !draft.existing_case) { setName(...); setKind(draft.default_expectation_kind); } }, [draft?.finding_id])` — deliberately keyed ONLY on `finding_id` (per its own comment, "so an in-flight edit isn't clobbered by a refetch of the same finding"). Repro: open the modal (finding accepted → "Must find" checked) → `unmount()` (mirrors `FindingsPanel`'s `{evalCaseFindingId && <EvalCaseModal/>}` closing) → change the mocked draft's `default_expectation_kind` to `must_not_flag` (mirrors dismissing the finding) → reopen on the SAME `findingId`, SAME `QueryClient`. Debug instrumentation on this exact repro showed: the radio is present and CHECKED (`must_find`) synchronously on the very first render of the new mount — TanStack Query serves the cached (stale) draft synchronously to a fresh `useQuery` subscriber before its `refetchOnMount: "always"` background fetch resolves; `qc.getQueryState(...)` at that instant shows `fetchStatus: "fetching"` (refetch genuinely in flight) with `data.default_expectation_kind` still `"must_find"`. The refetch DOES complete and DOES update the cache to `"must_not_flag"` (confirmed via `get.mock.calls.length` incrementing and cache inspection), but since `draft?.finding_id` is unchanged across that update ("f1" both times), the seeding effect never re-fires, and `kind` stays stuck at the FIRST open's value. `providers.tsx` sets no `gcTime` override (default 5 min), so this isn't a narrow race — any reopen of the same finding within 5 minutes of the first hits it. **This is a residual, currently-unfixed gap distinct from the query-level fix that was already shipped** — belongs to `implementer`, not `test-writer`: the seeding effect needs to also react to the draft's OWN `default_expectation_kind`/`suggested_name` changing (not just `finding_id`), while still not clobbering an in-flight edit — e.g. only re-seed when `kind`/`name` haven't been touched by the user yet, or reset a "seeded" ref when the query starts a genuinely fresh fetch (`isLoading` transition) rather than a background refetch of already-cached data.
- _2026-08-17_ — **`ContextAttachPanel.tsx`'s `role="checkbox"` leaf (moved there from the row per the same day's Codebase Patterns entry below) is itself unreachable by keyboard — it has no `tabindex` of its own, so `HTMLElement.focus()` on it is a documented no-op (verified: `document.activeElement` stays whatever it was, does not move to the span) and Tab never lands on it.** WAI-ARIA's checkbox pattern requires `tabindex="0"` on the SAME element that carries `role="checkbox"`; here that element is a leaf `<span>` while the actual (and only) Tab stop in the row is the row `<div>` one level up, which carries no role at all. Net effect: a keyboard/AT user tabbing through the list is never told "checkbox" or its checked state — the toggle is still operable (Enter/Space on the focused row still calls `toggle()`), but not correctly announced. Pinned by `ContextAttachPanel.test.tsx > makes every row control keyboard-reachable and accessibly named` (left red on purpose, mirroring how AC-16's test was previously green over a real defect — see `docs/plans/2026-08-17-context-attach-row-role-a11y.md`'s "The defect" section). Fix belongs to `implementer`: add `tabIndex={0}` to the checkbox `<span>` (and move the `Enter`/`Space` toggle handling onto it specifically, or keep it delegated from the row) so the accessible-role element and the actual Tab stop are the same node.
- _2026-08-16_ — **`useSetOwnerContext`'s optimistic update (`hooks/context.ts:71-101`) shows `token_count: 0` for a document that was NOT already in the owner's attached set, so the footer's combined token count in `ContextAttachPanel` does not move on a fresh attach while the PUT is still pending — only on detach/reorder does it move correctly.** `onMutate` builds the optimistic `attachments` array from `byPath = new Map(prev.attachments...)`; any `path` not found there (i.e. anything just toggled ON) falls through to the `token_count: 0` placeholder (line ~89) instead of looking up the real count from the sibling `["context","documents",repoId]` query cache, which DOES have it (that's exactly where `ContextAttachPanel`'s own per-row token count comes from, via `buildRows`/`docByPath` — so the ROW shows the right number immediately, only the FOOTER TOTAL lags). Confirmed with a live repro (mocked `put` held pending, asserted the footer text before resolving) before writing `ContextAttachPanel.test.tsx > updates the combined token count on attach and on detach without starting a run` — that test is written per spec/AC-10 and is left red on purpose; it passes for the detach half, fails for the attach half. Fix: `onMutate` needs the paths' current `token_count` from `qc.getQueryData(["context","documents",repoId])` for any path missing from `prev.attachments`, not a `0` placeholder.
