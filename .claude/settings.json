{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "bash -c 'S=\"$CLAUDE_PROJECT_DIR/scripts/pr-self-review-gate.sh\"; [ -r \"$S\" ] || { echo \"pr-self-review gate: $S is missing or unreadable — restore it, or remove this hook from .claude/settings.json deliberately.\" >&2; exit 2; }; exec bash \"$S\"'"
          }
        ]
      }
    ]
  }
}
