# server/specs — design specs

Design specs for features that are planned or in progress — the "why/what
we decided", written before or alongside implementation. Once a feature
ships, fold anything still true into the module's `CLAUDE.md` or
[docs/](../docs/) and either archive or delete the spec.

One file per feature (`0001-run-cost-badge.md`, …). Empty for now.
